<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kravel.server.auth.mapper.AccountMapper">
    <select id="selectAccount" parameterType="string" resultType="com.kravel.server.auth.model.Account">
		SELECT
			*
		FROM
			tb_account_main tam
		WHERE
			tam.LOGIN_EMAIL = #{loginEmail}
	</select>

</mapper>